// src/app/components/question.component.ts
import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-question',
  template: `
    <h2>Preguntas Aleatorias</h2>
    <!-- Tu contenido aquí -->
  `
})
export class QuestionComponent implements OnInit {
  ngOnInit() {
    // Inicialización aquí
  }
}
